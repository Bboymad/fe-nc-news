import { useEffect, useState } from 'react';
import { fetchData } from '../api';

const Header = () => {
    return (
        <div>
        <header>
          <h1>NC News</h1>
        </header>
      </div>
    );
};

export default Header;